1. **Blocker — the lid’s internal thread is opposite-handed after the lid is flipped.** `helix_ridge()` twists `-360*turns` ([lines 51–55](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:51)); both hull and lid call it with that hand ([72](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:72), [104](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:104)). But `assembly()` rotates the lid 180° about X ([150](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:150)), reversing its axial direction while preserving its angular direction. The physical lid groove therefore rises at `+360°/5 mm`; the hull ridge rises at `-360°/5 mm`. It cannot screw on. Add a handedness argument and use the opposite twist for the lid groove, e.g. `helix_ridge(..., handed=1)` for the hull and `handed=-1` for the lid.

2. **Blocker — the neck gasket groove cuts completely through the 2.4 mm wall and opens the hull.** At [80–84](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:80), the groove spans radii `47.5–50.1 mm`: `50 - 2.4/2 ± 2.6/2`. The cavity extends to `47.6 mm` ([78](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:78)), and the exterior is at `50 mm`; the cutter overlaps both. This creates an annular water path at `z=130.6–132 mm`. A 2.6 mm radial groove cannot fit in a 2.4 mm rim. At minimum, change to `groove_w = 1.4` (leaves 0.5 mm material on each side); preferably move the groove into the lid’s 3.9 mm radial sealing land and retain an uncut hull rim.

3. **Blocker — all three piezo retaining posts are disconnected floating solids, so they will print as islands.** The sinker ring ends at `z=10.99` ([140](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:140)); the posts begin at exactly `z=10.99` ([142](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:142)). More importantly, the ring occupies `r=13–14.6 mm`, while each post at radius 11 mm extends only to `r=12.75 mm`: a 0.25 mm radial gap. The cavity has already removed everything inside `r=19.6 mm` above `z=3` ([127](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:127)). Add real, overlapping bridges from the ring to each post—at least `0.4 mm` overlap—or redesign the ring with three inward tabs beneath the posts.

4. **Major — the lid groove’s axial offset leaves the bottom of the hull ridge ungrooved even aside from handedness.** Hull ridge: `z=116+2=118` through `130 mm` ([72](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:72)). In the assembled, inverted lid, the groove from [104](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:104) occupies `118.35–131.35 mm`: its `2-clr` start uses radial clearance as an axial shift. Thus the ridge’s lower 0.35 mm is not relieved. Once the lid hand is corrected, replace `translate([0,0,2 - clr])` with `translate([0,0,2])`; this gives a physical groove range `118–131 mm`. Keep `clr` only in diameters/radii.

5. **Major — the shown assembly is unseated by 1 mm, so it does not validate gasket compression or thread engagement.** The transform on [150](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:150) simplifies to an inverted lid whose underside is at `z=133 mm`; the hull rim is at `z=132 mm`. The displayed 1 mm air gap can hide an interference or falsely imply clearance. Replace the expression with the direct seated transform:
   ```scad
   translate([0,0,hull_h + lid_top_t])
     rotate([180,0,0]) lid_body();
   ```
   This puts the lid underside at `z=hull_h`. Remove the no-op `mirror([0,0,0])`.

6. **Major — the ESP32 “stop” intersects the displayed ESP32 rather than stopping it, and it is not tied to the bank-rib geometry.** The positive bank rib ends at `y=14.9 mm` ([89](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:89)). The stop begins at `y=20.5 mm` and occupies `x=-11.2..-8.8 mm` ([91](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:91)). The assembled ESP model is `x=-10..10`, `y=22.9..48.9` ([153](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:153)), so the rib penetrates it by 1.2 mm in X over most of its height. Move the stop outside the ESP envelope, e.g. to `x=-esp_h/2 - 1.2 .. -esp_h/2`, or make it a transverse end-stop at the actual board bottom.

7. **Major — the power-bank model does not fit the hull ID, and the ribs do not have clearance.** Hull ID is `2*(50-2.4)=95.2 mm`; the assembly’s bank is 100 mm wide ([152](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:152)), so it penetrates the wall by 2.4 mm on both sides. The bank-rib inside faces are exactly 25 mm apart, equal to `bank_t`, with zero assembly tolerance. This invalidates the internal layout. Either orient the real bank so its 65 mm dimension spans the hull, or set the actual packed envelope and make rib inside-face spacing `bank_t + 1–1.5 mm`.

8. **Major — the vertical cage slots clip the last ~1 mm of every piezo post.** Posts run `z=10.99–15.99` ([142](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:142)); slots begin at `z=15` ([130](/Users/mallhw/.claude/jobs/c05f918e/tmp/HackMIT/hardware/buoy-v2/buoy_v2.scad:130)). Slot angles include `0°, 120°, 240°`, exactly the post angles, so each post’s upper ~0.99 mm is cut. The slots do **not** cut the sinker ring (`ring z=2.99–10.99`) or top spokes (`slots end z=42`, top openings start `46.99`), but they do weaken/delete the post tips. Start slots at `head_floor + sinker_h + 5` (16 mm) or rotate slot angles by 30°.

I could not run the requested Manifold check because this environment’s `openscad` binary fails at startup with a Qt CPU-feature error; the findings above are from the evaluated source geometry.